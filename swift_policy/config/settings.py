class AlgorithmParams:
    def __init__(self, algorithm, params_yaml):
        print("Loading [%s] params from [%s]" % (algorithm, params_yaml))
        # Loading PPO parameters from file


class Settings:
    def __init__(self, root_dir, settings_yaml, rl_mode=RlMode.TRAIN):
        # Loading training settings from file
        pass
